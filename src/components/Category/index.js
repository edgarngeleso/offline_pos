import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Category = ({name,color,created_at=null}) => {
  return (
    <View>
      <Text>Category</Text>
    </View>
  )
}

export default Category;

const styles = StyleSheet.create({})